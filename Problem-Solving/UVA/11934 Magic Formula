#include<bits/stdc++.h>
using namespace std;
int main ()
{
    int a,b,c,d,l,cnt,ans;
while(1)
{ans=0,cnt=0;
    cin>>a>>b>>c>>d>>l;
    if ( a==0 && b==0 && c==0 && d==0&& l==0)
       {break;
       }

    for ( int i=0;i<=l;i++)
    {
        ans=a*pow(i,2)+b*i+c;
        if (ans%d==0)
            {cnt++;}

    }

    cout<<cnt<<'\n';
}


    return 0;
}
